/build/
/.cache/

# Leftover staging area from the dependency refresh. folly is no longer a
# dependency (see sources/utility/atomic_bit_set.h); the directory can be
# deleted whenever convenient.
/thirdparty_new/

# Commit message drafts handed over for manual signing
/COMMIT_MSG.txt
